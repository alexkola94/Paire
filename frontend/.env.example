# Backend API (optional; defaults to same origin in production)
# VITE_BACKEND_API_URL=http://localhost:5038

# Mapbox (for maps and geocoding)
# VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token

# Flight status (AeroDataBox via RapidAPI)
# VITE_AERODATABOX_API_KEY=your_rapidapi_key

# Transport booking is via external links only; no API keys needed.

# Other optional keys (discovery, notifications, etc.)
# VITE_SERPAPI_API_KEY=
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
# VITE_VAPID_PUBLIC_KEY=
