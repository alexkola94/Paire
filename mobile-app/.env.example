# API base URL for the .NET backend.
# On a real device (iPhone/Android), localhost points to the device, not your PC — use your PC's IP.
# Run: npm run env:local (or node scripts/set-api-url.js) to auto-detect and set this.
# No trailing slash. Example: http://192.168.1.100:5038
EXPO_PUBLIC_API_URL=http://192.168.x.x:5038
